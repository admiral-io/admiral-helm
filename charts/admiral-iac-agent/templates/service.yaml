apiVersion: v1
kind: Service
metadata:
  name: {{ include "admiral-iac-agent.fullname" . }}
  labels:
    {{- include "admiral-iac-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "admiral-iac-agent.selectorLabels" . | nindent 4 }}
