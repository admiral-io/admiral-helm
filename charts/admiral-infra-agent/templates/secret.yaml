{{- if and (not .Values.agent.existingSecret) .Values.agent.token }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "admiral-infra-agent.tokenSecretName" . }}
  labels:
    {{- include "admiral-infra-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  runner-token: {{ .Values.agent.token | quote }}
{{- end }}
