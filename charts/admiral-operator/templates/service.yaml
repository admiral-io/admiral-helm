apiVersion: v1
kind: Service
metadata:
  name: {{ include "admiral-operator.fullname" . }}
  labels:
    {{- include "admiral-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "admiral-operator.selectorLabels" . | nindent 4 }}
