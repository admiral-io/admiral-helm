apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admiral-operator.fullname" . }}-test-connection"
  labels:
    {{- include "admiral-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "admiral-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
