{{- if and .Values.dex.enabled .Values.dex.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "admiral.dex.serviceAccountName" . }}
  labels:
    {{- include "admiral.dex.labels" . | nindent 4 }}
  {{- with .Values.dex.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
